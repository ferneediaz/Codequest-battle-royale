# Supabase credentials
VITE_SUPABASE_URL=your-project-url
VITE_SUPABASE_ANON_KEY=your-anon-key

# To use Supabase authentication with GitHub and Google:
# 1. Create an account at https://supabase.com
# 2. Create a new project
# 3. Go to Project Settings -> API
# 4. Copy the URL and anon key and paste them here
# 5. Rename this file to .env.local (which should be git-ignored)
# 6. In Authentication -> URL Configuration, add your app URL to the Redirect URLs
#    (e.g., http://localhost:5173, or your deployed URL)
# 7. Enable GitHub and Google auth providers in Authentication -> Providers
#
# GitHub OAuth App Setup:
# 1. Go to GitHub -> Settings -> Developer settings -> OAuth Apps -> New OAuth App
# 2. Fill in Application name, Homepage URL (your app URL)
# 3. Set Authorization callback URL to: https://[YOUR-PROJECT-ID].supabase.co/auth/v1/callback
# 4. Copy the Client ID and Client Secret to the GitHub provider settings in Supabase
#
# Google OAuth Setup:
# 1. Go to Google Cloud Console -> Create Project (or use existing)
# 2. Go to APIs & Services -> Credentials -> Create OAuth Client ID
# 3. Configure consent screen if prompted
# 4. Create Web application OAuth client
# 5. Add authorized redirect URI: https://[YOUR-PROJECT-ID].supabase.co/auth/v1/callback
# 6. Copy the Client ID and Client Secret to the Google provider settings in Supabase 